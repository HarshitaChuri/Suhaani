# Drop your trained model file here: pcos_screening_model.joblib
#
# Export it from your research notebook with:
#   import joblib
#   joblib.dump(your_trained_random_forest_model, 'pcos_screening_model.joblib')
#
# The service auto-detects this file on startup and switches out of
# MOCK MODE automatically -- no code changes needed once it's here.
